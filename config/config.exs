import Config

# Each demo remains self-contained under apps/. Add one import when introducing
# a version app (`vX_Y_Z`) or a feature app (`f_<feature>`).
import_config "../apps/v0_1_0/config/config.exs"
import_config "../apps/f_image_sources/config/config.exs"
